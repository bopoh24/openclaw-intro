# Terraform state (secrets — do not commit)
terraform/terraform.tfvars
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/.terraform/

# OpenClaw runtime data
.env
config/
workspace/

# Live server config (pulled via `task config:pull`, not committed)
openclaw.json

# OS
.DS_Store
.idea
